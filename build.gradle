/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * User Manual available at https://docs.gradle.org/5.2/userguide/tutorial_java_projects.html
 */
// plugins {
//     id 'com.avast.gradle.docker-compose' version '0.9.4'
// }
// allprojects {
subprojects {
    repositories {
        // Use jcenter for resolving your dependencies.
        // You can declare any Maven/Ivy/file repository here.
        jcenter()
        mavenCentral()
        maven {
            name = 'clojars'
            url = 'https://clojars.org/repo/'
        }
        maven {
            url = 'https://repo.spring.io/milestone'
        }
    }
}

ext {
//    set('springCloudVersion', "Greenwich.RELEASE")
    set('springCloudVersion', "Hoxton.RC1")
}

wrapper {
    // gradleVersion='5.5'
    gradleVersion='6.0'
}
// dockerCompose{
//     vagrant{
//         environment.put 'DOCKER_HOST', 'tcp://172.30.1.5:2376'
//         useComposeFiles=['docker-compose-vagrant.yml']
//         composeLogToFile = project.file('out/log.log')
//         forceRecreate = true
//         buildBeforeUp=true
//         buildBeforePull=true
//         ignorePullFailure = false
//         ignorePushFailure = false
//     }
// }

group = 'au.edu.uq'
version = '0.0.1-SNAPSHOT'
//some text

fileTree('external_resources'){
    FileTree fileTree->fileTree.visit{
        FileTreeElement element->
        println "the filename is: ${element.file.name}"

    }
}

task helloTask(group:'uq', description:'uq tasks'){
    println "I am uq task"
    doFirst{
        println "group:${group}"
    }
    doLast{
        println "description:${description}"
    }
}
